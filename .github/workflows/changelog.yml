name: changelog

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  changelog:
    name: Legacy workflow disabled
    runs-on: ubuntu-latest
    steps:
      - name: Use the independent release workflow
        run: echo 'Automatic changelog publication is disabled; use .github/workflows/release.yaml.'
