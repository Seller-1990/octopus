---
status: accepted
---

# Default to strict protocol compatibility

Octopus selects same-protocol passthrough first and permits protocol transformation only when the request features are verified as lossless. Lossy conversion is an explicit per-Channel or per-Canonical Model choice with a preview and audit warning, so tools, structured output, multimodal content, cache semantics, and continuation state are never silently discarded.
